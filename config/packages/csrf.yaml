# Use classic session-based CSRF protection (better stability with EasyAdmin forms)
framework:
    form:
        csrf_protection:
            token_id: submit

    csrf_protection:
        stateless_token_ids: []
